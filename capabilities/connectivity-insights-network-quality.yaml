naftiko: 1.0.0-alpha2
info:
  label: Global System for Mobile Communications GSMA Camara Project Connectivity Insights API — Network Quality
  description: 'Global System for Mobile Communications GSMA Camara Project Connectivity Insights API — Network Quality. 1
    operations. Lead operation: Global System for Mobile Communications Check the network quality.. Self-contained Naftiko
    capability covering one Global System For Mobile Communications business surface.'
  tags:
  - Global System For Mobile Communications
  - Network Quality
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GLOBAL_SYSTEM_FOR_MOBILE_COMMUNICATIONS_API_KEY: GLOBAL_SYSTEM_FOR_MOBILE_COMMUNICATIONS_API_KEY
capability:
  consumes:
  - type: http
    namespace: connectivity-insights-network-quality
    baseUri: ''
    description: Global System for Mobile Communications GSMA Camara Project Connectivity Insights API — Network Quality business
      capability. Self-contained, no shared references.
    resources:
    - name: check-network-quality
      path: /check-network-quality
      operations:
      - name: checknetworkquality
        method: POST
        description: Global System for Mobile Communications Check the network quality.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
  exposes:
  - type: rest
    namespace: connectivity-insights-network-quality-rest
    port: 8080
    description: REST adapter for Global System for Mobile Communications GSMA Camara Project Connectivity Insights API —
      Network Quality. One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/check-network-quality
      name: check-network-quality
      description: REST surface for check-network-quality.
      operations:
      - method: POST
        name: checknetworkquality
        description: Global System for Mobile Communications Check the network quality.
        call: connectivity-insights-network-quality.checknetworkquality
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connectivity-insights-network-quality-mcp
    port: 9090
    transport: http
    description: MCP adapter for Global System for Mobile Communications GSMA Camara Project Connectivity Insights API — Network
      Quality. One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: global-system-mobile-communications-check
      description: Global System for Mobile Communications Check the network quality.
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: connectivity-insights-network-quality.checknetworkquality
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
